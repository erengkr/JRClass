import { OgrenciFilterPipe } from './ogrenci-filter.pipe';

describe('OgrenciFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OgrenciFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
